/// API route stubs for cloud server.
/// Routes mirror the existing Python FastAPI endpoints.
/// All business logic delegates to asterion-core.

pub struct ServerState;

impl ServerState {
    pub fn new() -> Self {
        Self
    }
}
